<h2>New Book</h2>
<%= form_with model: @books, url: '/books/index', method: :post do |f| %>
<h4>title</h4>
<%= f.text_field :title %>

<h4>body</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>

<% end %>